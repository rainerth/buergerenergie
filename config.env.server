# Konfigurationsdatei für Bürgerenergie Hugo Site - SERVER VERSION
# Diese Datei wird vom Build-Script gelesen um umgebungsspezifische Pfade zu setzen

# CSV-Datei Speicherort (wird für Symlinks verwendet)
# Server-Umgebung:
CSV_SOURCE_PATH=/home/pacs/wme00/users/buergerenergie/interessenten.csv

# Build-Verzeichnisse (normalerweise nicht ändern)
ASSETS_CSV_PATH=assets/data/interessenten.csv
STATIC_CSV_PATH=static/intern/interessenten.csv

# Apache .htpasswd Pfad für Server (wird in .htaccess verwendet)
HTPASSWD_SERVER_PATH=/var/www/html/intern/.htpasswd
